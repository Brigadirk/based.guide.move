# Backend Tester Environment Variables

# Environment (automatically detected)
NODE_ENV=production

# Backend URL (automatically configured based on NODE_ENV)
# Development: http://localhost:5001
# Production: http://bonobo-backend.railway.internal
# Override only if using custom backend URL
# NEXT_PUBLIC_BACKEND_URL=http://your-custom-backend-url

# Optional: Custom port for local development
# PORT=3001
