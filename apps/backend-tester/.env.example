# Backend Tester .env example

# Preset backend URLs for quick switching
NEXT_PUBLIC_LOCAL_URL=http://127.0.0.1:5001
NEXT_PUBLIC_API_URL=https://your-backend.up.railway.app
NEXT_PUBLIC_STAGING_API_URL=https://backend-staging.example.com
NEXT_PUBLIC_PRODUCTION_API_URL=https://backend.example.com

# API keys the tester proxy will attach (priority: PROD > STAGING > LOCAL).
# You can also override per-request using the x-backend-api-key header.
PRODUCTION_API_KEY=
STAGING_API_KEY=
LOCAL_API_KEY=

# Notes:
# - The tester UI sets x-backend-base-url automatically when you pick a preset.
# - Restart the dev server after changing env values.